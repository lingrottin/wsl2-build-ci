<script>
  // Initialize ripples
  const selects = Array.prototype.map.call(
    document.querySelectorAll('.mdc-select'),
    function (el) {
      return new mdc.select.MDCSelect(el);
    }
  );
</script>
