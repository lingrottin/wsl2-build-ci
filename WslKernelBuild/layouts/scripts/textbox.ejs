<script>
  // Initialize textboxs
  const textFields = Array.prototype.map.call(
    document.querySelectorAll('.mdc-text-field'),
    function (el) {
      return new mdc.textField.MDCTextField(el);
    }
  );
</script>
